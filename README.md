# gitui.nvim

gitui.nvim is nvim plugin that shows the [gitui](https://github.com/gitui-org/gitui) in the floating window.

## Requirements

- gitui installed :)

## Configuration

None for now, will be in future.

## Mapping

`<leader>gg` toggles the window
